#include "main.h"
#include <stdlib.h>
/**
 * malloc_checked - a function to allocate memory
 * @b: unsigned inteeger
 * Return: a pointerg void
 */
void *malloc_checked(unsigned int b)
{
	vod *g;

	g = malloc(b);

	if (g == NULL)
		exit(98);

	return (p);
}
